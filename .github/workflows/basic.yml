name:basic

on: push

jobs:
build_and_test:
    name: Build and test
    runs-on: ubuntu-latest

steps:
      -name: Checkout code
       uses: actions/checkout@v2
      -name: Run basic
       uses: github/basic@v3
 env:
 DEFAULT_BRANCH: MAIN
GITHUB_TOKEN: ${{secrets.GITHUB_TOKENS}}
